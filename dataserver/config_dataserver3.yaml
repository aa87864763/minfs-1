# DataServer 3 Configuration
server:
  listen_address: "0.0.0.0:8003"
  dataserver_id: "dataserver-03"

storage:
  data_root_path: "./data3"
  max_storage_size: 0
  block_size: 4194304

etcd:
  endpoints: ["localhost:2379"]
  dial_timeout: 5
  lease_ttl: 30

metaserver:
  address: "localhost:9090"
  heartbeat_interval: 10
  connection_timeout: 5

logging:
  level: "info"
  output: "stdout"